plugins {
    id 'java'
    id 'application'
}

group 'bsu.comp152'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation("org.junit.jupiter:junit-jupiter-api:5.8.2", "org.hamcrest:hamcrest:2.2")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:5.8.2")
}

test {
    useJUnitPlatform()
    testLogging {
        events "passed", "failed"
	    exceptionFormat "full"
	    ignoreFailures = true
	    showStandardStreams = true
    }
}

mainClassName = 'bsu.comp152.InsulinDosage'

